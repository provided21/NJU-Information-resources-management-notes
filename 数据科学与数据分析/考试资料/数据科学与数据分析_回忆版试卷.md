# 数据挖掘与 R 语言 回忆版试卷（最终完整版）

> 说明：本卷基于考后复盘整理，仅列出题目框架与考点。具体数值、选项完整内容及图表因记忆模糊已标注“回忆版”，仅供参考题型与考察方向。代码补全题附参考答案。

## 一、选择题（约 10 分，每题 1 分）

### 1. 数据类型识别

同一时间、不同个体的数据属于什么数据类型？

A. 时间序列数据  
B. 截面数据  
C. 面板数据  
D. 纵向数据

### 2. 编辑距离

在忽略操作权重的情况下，将 `"travel"` 转换为 `"traffic"` 所需的编辑距离是多少？（选项略）

### 3. 拉普拉斯修正

进行四分类问题时，拉普拉斯修正的分母应加多少？（选项略）

### 4. 假设检验

关于假设检验，下列说法错误的是？

A. 第一类错误  
B. 第二类错误  
C. 假设检验的结论不是 100% 正确的  
D. 检验统计量用于和 p 值比较从而判断

### 5. ggplot2 美术映射

`ggplot2` 中，用于将数据映射到图形属性（颜色、大小等）的函数是？

A. `geom_xxx()`  
B. `aes()`  
C. `facet_wrap()`  
D. `theme()`

### 6. ARIMA 参数选择

给出一张 ACF/PACF 图（回忆版：差分一次后，ACF 在滞后 1 阶截尾，PACF 在滞后 2 阶截尾），问应选择哪个 ARIMA 模型？（选项略）

### 7. `nnet` 包的 `size` 参数

R 语言 `nnet` 包中，参数 `size` 表示什么？

A. 学习率  
B. 最大迭代次数  
C. 隐层节点数  
D. 权重衰减系数

### 8. One-Hot 编码的距离度量

对 One-Hot 编码后的高维稀疏二元数据，最适合用哪种距离？

A. 欧氏距离  
B. 曼哈顿距离  
C. 余弦相似度  
D. 切比雪夫距离

## 二、填空题（约 10 分，每题 1 分）

### 1. R 语言赋值符号

R 语言中最常用的赋值运算符是 ______。

### 2. 数据结构识别

执行 `a <- c(1, 2, 3)` 后，`a` 的数据结构是 ______。

### 3. 数据预处理

消除不同特征之间量纲影响的操作称为数据 ______。

### 4. 代码补全（无提示）

补全以下代码，使用 `recommenderlab` 包建立基于用户的协同过滤推荐模型：

```r
library(recommenderlab)
data <- as(_____, "realRatingMatrix")
model <- Recommender(data, method = "_____")
pred <- predict(model, newdata, n = 10)
```

参考答案：

- 填空 1：`train_data`（或实际对象名）
- 填空 2：`UBCF`

### 5. 代码补全

补全以下代码，使用 `e1071` 包建立朴素贝叶斯分类模型：

```r
library(e1071)
model <- _____(Species ~ ., data = iris)
pred <- predict(model, newdata)
```

参考答案：`naiveBayes`

### 6. 假设检验方向

若原假设为 $H_0: \mu \le a$，备择假设为 $H_1: \mu > a$，则该检验称为 ______ 检验。

### 7. SVM 间隔名称

SVM 中，支持向量所在的两条平行超平面之间的距离称为 ______。

### 8. 代码补全

补全以下代码，使用 `rpart` 包建立决策树模型（因变量为分类变量）：

```r
library(rpart)
model <- rpart(Species ~ ., data = iris, method = "_____")
```

参考答案：`"class"`（回归树填 `"anova"`）

## 三、简答题（30 分）

### 1. 特征选择的重要性与方法

1. 建模前进行特征选择为什么重要？
2. 列举常用的特征选择方法。

### 2. K-means 聚类的优缺点

简述 K-means 算法的优缺点。

### 3. 层次聚类的距离选择

层次聚类中，常用的簇间距离度量包括最近距离、最远距离、重心距离、平均距离等。

现需在森林火灾监测场景中尽可能发现异常着火点，应选择哪种距离？为什么？

## 四、计算题（30 分）

### 1. 方差分析表填表

某单因素方差分析（4 个处理组）的部分结果如下（数值为回忆版）：

| 来源 | SS（万） | df | MS（万） | F |
|---|---:|---:|---:|---:|
| 组间（策略） | 4.9 | ? | ? | ? |
| 组内（误差） | 11.5 | ? | ? | |
| 总计 | 22 | 25 | | |

1. 补全表格中的 `df`、`MS` 和 `F` 值。
2. 在 $\alpha = 0.05$ 下判断是否显著（已知 F 临界值约 3.22）。

### 2. 混淆矩阵计算

某二分类模型的混淆矩阵如下：

| 真实\预测 | 正例 | 负例 |
|---|---:|---:|
| 正例 | 30 | 10 |
| 负例 | 6 | 54 |

计算准确率、精确率、召回率和 F1 分数。

### 3. 关联规则挖掘

给定 10 条事务数据（回忆版，原题给出了完整事务列表），请回答以下问题：

1. 计算两个指定关联规则的支持度、置信度和提升度。
2. 自行寻找一条最优的关联规则，要求该规则中必须包含项 `a`。

## 五、函数诊断题（10 分）

### 1. VIF 多重共线性诊断

给出一张 VIF 诊断图（回忆版：图中各变量 VIF 值均小于 10），请判断该模型是否存在多重共线性问题，并说明理由。

### 2. 异常点诊断

给出 R 语言 `plot(lm())` 输出的四张诊断图（残差图、QQ 图、Scale-Location 图、Cook 距离图），请指出图中的异常点、高杠杆点和强影响点，并分别说明判断依据。
